# Dubai-Real-Estate-Data-Cleaning
Built a SQL data-cleaning pipeline for the Nashville Housing dataset. Standardized dates, filled missing addresses using JOINs on ParcelID, parsed address fields into columns, normalized Boolean values, removed duplicates using CTEs and ROW_NUMBER(), and optimized the schema with ALTER TABLE and DROP COLUMN for better performance
